import 'package:flutter/material.dart';

class WorkerAuthScreen extends StatelessWidget {{
  const WorkerAuthScreen({{super.key}});

  @override
  Widget build(BuildContext context) {{
    return Scaffold(
      appBar: AppBar(title: const Text('W o r k e r A u t h S c r e e n')),
      body: const Center(child: Text('W o r k e r A u t h S c r e e n')),
    );
  }}
}}
