import 'package:flutter/material.dart';

class WorkerSupportScreen extends StatelessWidget {{
  const WorkerSupportScreen({{super.key}});

  @override
  Widget build(BuildContext context) {{
    return Scaffold(
      appBar: AppBar(title: const Text('W o r k e r S u p p o r t S c r e e n')),
      body: const Center(child: Text('W o r k e r S u p p o r t S c r e e n')),
    );
  }}
}}
