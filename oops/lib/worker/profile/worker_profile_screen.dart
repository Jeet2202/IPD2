import 'package:flutter/material.dart';

class WorkerProfileScreen extends StatelessWidget {{
  const WorkerProfileScreen({{super.key}});

  @override
  Widget build(BuildContext context) {{
    return Scaffold(
      appBar: AppBar(title: const Text('W o r k e r P r o f i l e S c r e e n')),
      body: const Center(child: Text('W o r k e r P r o f i l e S c r e e n')),
    );
  }}
}}
