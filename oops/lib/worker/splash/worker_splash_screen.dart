import 'package:flutter/material.dart';

class WorkerSplashScreen extends StatelessWidget {{
  const WorkerSplashScreen({{super.key}});

  @override
  Widget build(BuildContext context) {{
    return Scaffold(
      appBar: AppBar(title: const Text('W o r k e r S p l a s h S c r e e n')),
      body: const Center(child: Text('W o r k e r S p l a s h S c r e e n')),
    );
  }}
}}
