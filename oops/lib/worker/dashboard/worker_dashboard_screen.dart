import 'package:flutter/material.dart';

class WorkerDashboardScreen extends StatelessWidget {{
  const WorkerDashboardScreen({{super.key}});

  @override
  Widget build(BuildContext context) {{
    return Scaffold(
      appBar: AppBar(title: const Text('W o r k e r D a s h b o a r d S c r e e n')),
      body: const Center(child: Text('W o r k e r D a s h b o a r d S c r e e n')),
    );
  }}
}}
