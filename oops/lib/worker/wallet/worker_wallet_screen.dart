import 'package:flutter/material.dart';

class WorkerWalletScreen extends StatelessWidget {{
  const WorkerWalletScreen({{super.key}});

  @override
  Widget build(BuildContext context) {{
    return Scaffold(
      appBar: AppBar(title: const Text('W o r k e r W a l l e t S c r e e n')),
      body: const Center(child: Text('W o r k e r W a l l e t S c r e e n')),
    );
  }}
}}
