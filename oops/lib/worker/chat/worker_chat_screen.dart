import 'package:flutter/material.dart';

class WorkerChatScreen extends StatelessWidget {{
  const WorkerChatScreen({{super.key}});

  @override
  Widget build(BuildContext context) {{
    return Scaffold(
      appBar: AppBar(title: const Text('W o r k e r C h a t S c r e e n')),
      body: const Center(child: Text('W o r k e r C h a t S c r e e n')),
    );
  }}
}}
