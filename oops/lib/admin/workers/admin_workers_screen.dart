import 'package:flutter/material.dart';

class AdminWorkersScreen extends StatelessWidget {{
  const AdminWorkersScreen({{super.key}});

  @override
  Widget build(BuildContext context) {{
    return Scaffold(
      appBar: AppBar(title: const Text('A d m i n W o r k e r s S c r e e n')),
      body: const Center(child: Text('A d m i n W o r k e r s S c r e e n')),
    );
  }}
}}
