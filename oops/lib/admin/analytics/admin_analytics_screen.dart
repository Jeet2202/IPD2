import 'package:flutter/material.dart';

class AdminAnalyticsScreen extends StatelessWidget {{
  const AdminAnalyticsScreen({{super.key}});

  @override
  Widget build(BuildContext context) {{
    return Scaffold(
      appBar: AppBar(title: const Text('A d m i n A n a l y t i c s S c r e e n')),
      body: const Center(child: Text('A d m i n A n a l y t i c s S c r e e n')),
    );
  }}
}}
